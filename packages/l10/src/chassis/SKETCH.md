# Jake Agent Production Chassis — Phase 1: Implementation

All core chassis modules with full type safety and hash-chained packets.

## Coordinate: L4-D2-A2-P (Plan → Build)

Building the irreducible production chassis that wraps every RIG agent run
with H=(E,T,C,S,L,V) enforcement, typed packets, workflow FSM, durable
state, tool classification, policy gates, and GEV separation.
